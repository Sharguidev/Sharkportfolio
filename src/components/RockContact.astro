<div class="container"  id="rock-contact">
    <div class="greeting ">
        <h1>Let’s Rock 🎸</h1>           
    </div>
    <div class="container-rock">
        <div class="card">
            <form name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field" data-netlify-recaptcha="true">
                <!-- <input type="hidden" name="form-name" value="contact" /> -->
                <div class="hidden">
                    <label>Don't fill if you are human <input name="bot-field" type="text"/></label>
                </div>
                <div class="mb-3 mt-3">
                <label for="name" class="form-label">Name</label>
                <input type="text" class="form-control" name="name" placeholder="John Doe" required>
                </div>
                <div class="mb-3">
                <label for="email" class="form-label">Email Address</label>
                <input type="email" class="form-control" name="email" placeholder="JoDOe@gmail.com" required>
                </div>
                <div class="mb-1">
                    <label for="message" class="form-label">Message</label>
                    <textarea  class="form-control" name="message" placeholder="Example: Hello! I was wonderig if we can schedule a google meet to talk about my project." required/>
                </div>
                <div class="captcha" data-netlify-recaptcha="true"></div>
                <button type="submit" class="btn-submit">Let’s Rock 🎸</button>
            </form>
        
        </div>
    </div>
</div>

<script>
    document.addEventListener('input', (event) => {
        const target = event.target as HTMLElement | null; // Explicitly type the target
        if (target && target.tagName.toLowerCase() === 'textarea') {
            const textarea = target as HTMLTextAreaElement; // Narrow the type to HTMLTextAreaElement
            textarea.style.height = 'auto'; // Reset height to calculate new height
            textarea.style.height = `${textarea.scrollHeight}px`; // Set height based on content
        }
    });
</script>


<style>

.hidden {
    display: none;
}    

.container{
    padding-top: 10px;
    padding-bottom: 100px;
}  

.container .greeting {
    text-align: end;
    margin: 10% 10% 2% 0;
}

.card
{
    width: 80%;
    height: fit-content;
    margin: 0 auto;
    padding: 10px 20px;
}

input[type="email"],
input[type="text"],
textarea {
    width: 80%;
    padding: 10px;
    border-radius: 5px;
    border: 1px solid #ccc;
    margin: 0 auto 10px auto;
    resize: none;
}

textarea {
    height: auto;
    overflow: hidden;
}

.greeting h1 {
    font-size: 1.75rem; /* Increase font size */
    font-weight: bold;
}

form .form-label {
    display: block;
    text-align: left;
    width: 80%;
    margin: 0 auto;
    margin-bottom: 5px;
}
  
form .form-label {
    display: block;
    text-align: left;
    width: 80%;
    margin: 0 auto;
    margin-bottom: 5px;
}

.btn-submit {
    margin: 1px 0 5px 0;
    background-color: #344E41;
    color: #ffffff;
    border: none;
    padding: 10px 10px;
    border-radius: 5px;
    cursor: pointer;
    width: fit-content;
    margin-bottom: 20px;
}

.captcha {
    margin: 5% auto;
    display: flex;
    justify-content: center;

/* tablet: up to 900px */
@media (max-width: 900px) {
  .container .greeting {
    text-align: center;
    margin: 8% auto 3% auto;
  }
  .greeting h1 {
    margin-left: 0;
    font-size: 1.5rem;
  }
  .greeting p {
    margin-left: 0;
  }
  .card {
    width: 95%;
    padding: 10px 5vw;
  }
  input[type="email"],
  input[type="text"],
  textarea,
  form .form-label {
    width: 95%;
    margin: 0 auto 10px auto;
    font-size: 1rem;
  }
  .btn-submit {
    width: 90%;
    margin: 10px auto 20px auto;
    display: block;
    font-size: 1rem;
  }
  .captcha {
    margin: 5% auto;
    justify-content: center;
  }
}

/* Mobile: up to 600px */
@media (max-width: 600px) {
   .container {
     padding-top: 5px;
     padding-bottom: 40px;
   }
   .card {
     width: 100%;
     padding: 10px 2vw;
   }
   .greeting h1 {
     font-size: 1.2rem;
   }
   input[type="email"],
   input[type="text"],
   textarea,
   form .form-label {
     width: 100%;
     font-size: 0.95rem;
   }
   .btn-submit {
     width: 100%;
     font-size: 0.95rem;
   }
 }
</style>